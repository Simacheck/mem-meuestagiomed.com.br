export const metadata = {
    title: 'Detalhes da Vaga | Meuestagiomed.com.br'
  }
  export default function ClientLayout({
    children,
  }: {
    children: React.ReactNode
  }){
    return children
  }